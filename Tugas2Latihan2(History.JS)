import React from 'react';
import {View, StyleSheet, SafeAreaView, Text} from 'react-native';

function History(){
    return (
        <View style={{borderWidth:1, width:120, height:220}}>
           <Text>A bicycle, also called a <Text style={{ color: 'red' }}>pedal cycle</Text>,
           <Text style={{fontWeight:'bold'}}>bike</Text>, push-bike or cycle, is a human-
           powered or motor powered assisted, pedal-driven, single-track vehicle, having two
           wheels attached to a frame, one behind the other. A bicycle rider is called a
           cyclist, or bicyclelist</Text> 
        </View>
    );
}

const styles = StyleSheet.create({})

export default History;
