import React from 'react';
import {View, StyleSheet, Text, Image, SafeAreaView} from 'react-native';

const App = () => {
  return (
    <SafeAreaView style={{flex:1, justifyContent:'center', flexDirection:'column', alignItems:'center'}}>
        <View style={{justifyContent:'center', flexDirection:'column'}}>
          <View SafeArstyle={{borderWidth:1, height:20, width:120, backgroundColor:'red'}}>
            <Text style={{fontWeight:'bold', textAlign:'center', color:'white'}}>History of Bicycle</Text>
          </View>  
          <View style={{flexDirection:'column', justifyContent:'center', alignItems:'center'}}>
          <View style={{borderWidth:1, width:120, height:220}}>
            <Text>A bicycle, also called a <Text style={{ color: 'red' }}>pedal cycle</Text>,
            <Text style={{fontWeight:'bold'}}>bike</Text>, push-bike or cycle,
              is a human-powered or motor powered assisted, pedal-driven, single-track
              vehicle, having two wheels attached to a frame, one behind the other.
              A bicycle rider is called a cyclist, or bicyclelist</Text>
          </View>
            <Image source={require('./assets/Image/Bicycle.jpg')} style={{width:100,height:100}}/>
          </View>
        </View>
      </SafeAreaView>
  );
}

const styles = StyleSheet.create({})

export default App;
